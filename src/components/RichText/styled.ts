import styled from "@emotion/styled";

export const A = styled.a`
  color: var(--color-blueish);
`;

export const Blockquote = styled.blockquote`
  border-left: 3px solid var(--color-blackish);
  border-radius: 3px;
  margin-left: 0;
  padding-left: 1rem;
`;
